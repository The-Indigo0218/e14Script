# Entorno
.venv/
__pycache__/
*.pyc

# Claves / secretos
.env
drive_credentials.json
.drive_token.json

# Datos y salidas generadas
*.db
*.bak
*.bak.*
*.xlsx
debug/
salidas/
sitio/
# Muestras demo movidas fuera del flujo (locales, no se suben)
muestras_demo/

# Actas reales de entrada (no subir por defecto; sí conservar las carpetas)
datos/**/*.pdf
datos/**/*.jpg
datos/**/*.png

# Sistema
.DS_Store

# Contexto de continuidad entre sesiones de chat (local, no se sube)
context.md
